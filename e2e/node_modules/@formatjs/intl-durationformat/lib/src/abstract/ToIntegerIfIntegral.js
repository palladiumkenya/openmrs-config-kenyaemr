import { ToNumber } from '@formatjs/ecma402-abstract';
export function ToIntegerIfIntegral(arg) {
    var number = ToNumber(arg);
    if (!Number.isInteger(number)) {
        throw new RangeError("".concat(arg, " is not an integer"));
    }
    return number;
}
