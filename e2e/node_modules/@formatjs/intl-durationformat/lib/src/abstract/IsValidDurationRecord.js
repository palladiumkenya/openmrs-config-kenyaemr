import { invariant } from '@formatjs/ecma402-abstract';
import { TABLE_1 } from '../constants';
import { DurationRecordSign } from './DurationRecordSign';
export function IsValidDurationRecord(record) {
    var sign = DurationRecordSign(record);
    for (var _i = 0, TABLE_1_1 = TABLE_1; _i < TABLE_1_1.length; _i++) {
        var key = TABLE_1_1[_i];
        var v = record[key];
        invariant(isFinite(Number(v)), "".concat(key, " is not finite"));
        if (v < 0 && sign > 0) {
            return false;
        }
        if (v > 0 && sign < 0) {
            return false;
        }
    }
    return true;
}
